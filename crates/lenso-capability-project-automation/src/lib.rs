//! Portable, bounded automation contract for Lenso Projects.

// The generated native dispatcher has one match arm per declared operation.
#![allow(clippy::too_many_lines)]

include!("generated.rs");
